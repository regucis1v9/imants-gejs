import React, { useState } from 'react';
import MapContainer from './MapContainer'; // Import the MapContainer component

const CreateEvent = () => {
    return (
        <div className="account-component-main">
            <MapContainer />
        </div>
    );
}

export default CreateEvent;
